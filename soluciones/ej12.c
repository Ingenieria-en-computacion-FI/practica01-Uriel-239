
#include <stdio.h>
int main(){
/*
Se sabe:
int m[3][5];
base = 4000

I. A grandes rasgos, el valor resultante sería 4056; sin embargo, al realizar el análisis correcto bajo el sistema hexadecimal, el valor obtenido es 403A.
II. En este caso, el resultado sería 4032. Al igual que en el punto anterior, si lo convertimos a hexadecimal, el valor final queda representado como 4020.
III. La diferencia es de 24 bits. Este cálculo se obtiene gracias al conocimiento del "peso" o tamaño que ocupa en memoria un dato de tipo entero.
*/

    return 0;
}
